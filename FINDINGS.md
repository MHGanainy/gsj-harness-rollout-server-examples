# FINDINGS — the external register

Frictions hit while consuming `gsj-harness-rollout-server` from the
outside. Severity: BLOCKER (cannot proceed), FRICTION (workaround
needed), DOC (surprising but documented here), COSMETIC. "library-side?"
marks findings the library should absorb. UNVERIFIED rows are reported,
not confirmed — the predecessor repo's rule: unverified is not false.

| # | severity | finding | library-side? | status |
| --- | --- | --- | --- | --- |
| F-01 | BLOCKER | `pins/` did not ship in the wheel: `checks.PINS_PATH` resolved into site-packages and every trainer-side `validate_session_result` raised `PinsConfigurationError` — law 6's trainer leg was non-functional from an installed wheel (first recorded at library CP-11b, dispositioned then as "both legs run from the checkout by design") | yes | **FIXED at library CP-16** (ADR-0017: packaged pins + `GSJ_PINS_PATH` override; proven from a scratch venv against the real CP-09′ body) |
| F-02 | DOC | `reward` is `null` on every real callback body to date (CP-06/07/09/09′). The bridge consumes what the evaluator placed — `null` → `0.0` under the reward key — so a loop run today trains on zero reward everywhere and LOO advantages are identically zero. A reward/evaluator attach step is a named CP-17 input, not a bridge feature (the scope law: scoring is out) | no — corpus/evaluator question | OPEN, CP-17 input |
| F-03 | FRICTION | slime is not importable off-estate (THUDM git + Megatron + torch; the unrelated PyPI `slime` is 0.0.0), and the vendored Polar adapter hard-raises without it. The bridge carries `FallbackSample`, a test double mirroring exactly the constructor surface the vendored adapter uses, so its assertions are testable fixture-driven. The loop must run with real slime | no — deliberate consumer choice, documented | ACCEPTED |
| F-04 | DOC | The vendored adapter passes `session_id=` (and reads `group_id`, `remove_sample`, `Status.FAILED`) on `slime.utils.types.Sample` — a surface we can only verify against real slime v0.3.0 (+ the router-tokens patch) on-estate. If the real constructor rejects any of these, the bridge fails loudly at the first conversion | unverifiable here | UNVERIFIED until CP-17 (library A-26) |
| F-05 | DOC | The vendored placeholder hardcodes `tokens=[0, 0]` / `response_length=1` — assumes token id 0 is batcher-safe for the served vocabulary. Kept verbatim in the bridge (deviating from the template needs on-estate evidence); flagged so CP-17 knows where it lives | no | OPEN, CP-17 watches — **not exercised**: all 27 CP-17 episodes had a usable trace, so the placeholder path never ran |
| F-06 | FRICTION | Polar's `examples/swegym_slime_grpo/launch_e2e.sh` defaults `MEGATRON_REF=26.04-alpha.rc1` with the comment "Slime v0.3.0 imports megatron.training.tokenizer, which current Megatron main removed. Keep the default pinned to the compatible 26.04 alpha line." That tag does **not** carry the module (`megatron/training/` exists, `megatron/training/tokenizer/` does not; `_vocab_size_with_padding` survives only in a test), so `convert_hf_to_torch_dist.py` dies at import with `ModuleNotFoundError: No module named 'megatron.training.tokenizer'`. Cure: use the Megatron the `slimerl/slime:v0.3.0` image ships (`/root/Megatron-LM` @ `1dcf0dafa`, the Dockerfile's own `MEGATRON_COMMIT`) — the pair the image was built and tested with | no — Polar-side (a stale pin in an example) | OPEN, worked around (CP-17 uses the image's Megatron) |
| F-07 | FRICTION | slime v0.3.0 **silently no-ops the entire train loop** when `--load` points at a checkpoint carrying `latest_checkpointed_iteration.txt` and `--num-rollout` is small: the load is read as a resume (`start_rollout_id = loaded_rollout_id + 1`, `actor.py:99`), so `range(start_rollout_id, num_rollout)` = `range(1, 1)` = empty. The Ray job reports **SUCCESS**, weights load, `update_weights` runs, no step is taken and no checkpoint is written — the only symptom is a missing `latest_checkpointed_iteration.txt` in `--save`. Cure: pass `--start-rollout-id 0` explicitly for a fresh run | no — slime-side (a loud-failure candidate: an empty train range deserves a warning) | OPEN, worked around (CP-17 passes the flag) |
| F-08 | **BLOCKER-class numerics** (worked around) | Polar's vendored `slime_bridge/reward_post_process.py` explodes the advantage scale on sparse binary reward. Its leave-one-out scale is `statistics.stdev(other_means) + 1e-6`; when every *other* trajectory carries the same reward — the common case at demo scale, e.g. 1 rewarded episode in 27 — `stdev == 0` exactly and the scale collapses to the `1e-6` epsilon, so that trajectory's advantage becomes `(1.0 − 0.0)/1e-6 = 1e6`. Measured live at CP-17: `rollout/advantages` mean **37036.85** against `raw_reward` 0.037, `train/loss −37026.5`, `train/grad_norm 450053`. Megatron's `clip_grad 1.0` bounded the actual update, so this degrades to "one outlier dominates the step direction" rather than diverging — but nothing warns. Cure applied: `--disable-grpo-std-normalization` (Dr.GRPO), which yields `grad_norm 0.4513` and LOO-centered advantages summing to ~0. The principled fix is a degenerate-variance guard (skip normalization, or zero the advantage, when `stdev ≈ 0`) rather than an epsilon floor | no — Polar-side (the vendored post-processor) | OPEN, worked around + both runs recorded |
| F-09 | DOC (the F-08 question, asked of verl) | verl @ `1ae9455` does NOT have the F-08 shape, despite textually identical `(score − mean)/(std + 1e-6)` in `compute_grpo_outcome_advantage` (`core_algos.py:326`): its std is over the WHOLE uid group, self included, so `std == 0` implies every deviation is exactly 0 — the 1/ε factor only ever multiplies zero. With the CP-17 distribution (1 rewarded in 27, one group) max abs advantage is **≈5.0**, not 1e6; with all-identical rewards the advantages are exactly 0. verl's actual leave-one-out estimator (`compute_rloo_outcome_advantage`, `core_algos.py:588`) centres by the LOO mean and never divides by a variance (its `epsilon` parameter is dead code), so the F-08 mechanism has no counterpart to fire. Both behaviours pinned as executable tests (`test_f08_shape_verl_grpo_is_structurally_immune`, `test_verl_rloo_has_no_variance_division`) | no — verl-side, and it's the good kind | CLOSED at CP-20 (measured, test-pinned) |
| F-10 | DOC (CP-21 input) | verl hardcodes `mean = 0.0, std = 1.0` for a uid group of ONE (`core_algos.py:315-317`), so a singleton group's GRPO advantage is its RAW UNCENTRED reward — no baseline at all. The bridge's default uid (= session id) makes EVERY group a singleton at CP-17's collection shape (one episode per submission); a naive CP-21 loop would train on raw rewards while looking like GRPO. Grouping is therefore a named loop input: episodes of one prompt must share a uid, and the collection cadence must produce n > 1 per prompt. Pinned as `test_singleton_uid_group_gets_raw_reward_advantage` | no — verl semantics + loop design | **CLOSED at CP-21**: the loop ingests every episode of the one prompt under a single shared uid (`cp21-golden`, asserted in the harness) — one GRPO group of 110, advantages measured min −0.0953 / mean 0.0000 / max +10.39; the refused default was demonstrated live in the smoke run (a singleton group got advantage = raw reward 1.0 exactly) |
| F-11 | DOC (CP-21 fork) | verl @ `1ae9455` is mid-migration between trainer generations, and the boundary finding generalizes: `main_ppo` defaults to the v1 TransferQueue pipeline (`trainer.use_v1: true`), whose records are unpadded nested tensors written by clients that must run INSIDE the trainer's Ray cluster after `tq.init()` (TransferQueue==0.1.8), with trainer-minted uids and an unconditionally-read tag schema — **externally-built batches cannot enter it**. The padded `[B, L]` DataProto the bridge builds is the classic (`main_ppo_v0`) contract: deprecated upstream, still documented, and the only batch an external process can hand over (or: a custom `AgentLoop` returning `AgentLoopOutput` lets verl drive and do the padding itself). Also load-bearing: the v1 engine normalizes token-mean loss by `loss_mask` while classic losses read `response_mask` — the bridge emits both, equal, like verl's own TQ writer ("TODO: uniform response_mask and loss_mask") and uni-agent. One adjacent numeric hazard for the ERROR-row design: OPO (non-default estimator) divides by the group's summed mask lengths — a uid group whose members are ALL fully masked yields a 0/0 NaN baseline; GRPO/RLOO are immune | no — verl-side migration state; CP-21 must pick the fork deliberately | **ANSWERED at CP-21**: classic batch chosen (ADR-0004) and run — with the desk sharpening that at this pin even the classic v0 trainer runs on the unified model-engine workers (`DataParallelPPOActor` is gone), so the classic feed mirrors the v0 fit loop's plumbing around `TrainingWorker` directly; the v1/AgentLoop forks remain deliberately untested (different experiments) |
| F-12 | FRICTION | verl's classic-path conversion helper (`workers/utils/padding.py::left_right_2_no_padding` — the function the v0 fit loop runs on EVERY batch before `infer_batch`/`train_mini_batch`) hard-requires flash-attn: `verl/utils/attention_utils.py` is a dispatcher that resolves `unpad_input`/`pad_input`/`index_first_axis` to `flash_attn.bert_padding` on CUDA, lazily at first call, with **no torch fallback** — even when the engine itself is configured onto its flash-free branch (`use_remove_padding=False` + `override_config={"attn_implementation": "sdpa"}`), which needs no flash-attn anywhere (it builds its own attention mask via `build_attention_mask_from_nested`, pure torch, and `logprobs_from_logits` falls back to log_softmax+gather). On a host with no nvcc and no flash-attn wheel (the H200 estate), the v0 trainer's own batch plumbing is un-runnable as written. Worked around in `verl_bridge/cp21_loop/train_one_step.py::_to_no_padding`: a pure-torch reproduction of the helper's output contract (nested jagged `input_ids`/`position_ids`, `loss_mask` = `response_mask` alias, `max_seq_len`/`max_response_len` non-tensors), contract-asserted row by row at run time. The adjacent trap, found at the desk before it could burn a run: with `use_remove_padding=True` under sdpa the engine packs the batch to `1 × total_nnz` and passes `attention_mask=None` — sdpa then silently attends ACROSS sequence boundaries (wrong logprobs, no crash); flash-free operation REQUIRES `use_remove_padding=False` | no — verl-side (a pure-torch fallback for the three `bert_padding` functions would close it) | OPEN, worked around at CP-21 (the workaround ran the CP-21 loop) |
| F-13 | FRICTION | verl's OOM-guard for entropy is dead config on the flash-free branch: `FSDPEngineConfig.entropy_from_logits_with_chunking` is honoured only in the remove-padding branch of `prepare_model_outputs` (`workers/engine/fsdp/transformer_impl.py:1310-1314` chunks; the non-rmpad branch at `:1416-1420` calls the unchunked `verl_F.entropy_from_logits(logits)` unconditionally — only `entropy_checkpointing` is consulted there). Since F-12 forces a flash-attn-free host onto exactly that branch, the two findings compound: the branch you must use is the branch whose entropy memory guard silently does nothing, and the non-rmpad path additionally materializes logits at PADDED width (memory scales with the longest row in the micro-batch, not real tokens). Measured at CP-21, twice: a 110-row batch with a 30.7k-token row OOM'd an H200 (139.81 GiB) in `entropy_from_logits` with 106-114 GiB already in use — with the chunking flag SET. Worked around by dropping the entropy metric from the recompute pass (`calculate_entropy=False` — a deviation from the fit loop's `True`, decorative-metric-only); `entropy_checkpointing` is the in-tree alternative not taken (recompute-in-backward machinery for a forward-only metric) | no — verl-side (honour the chunking flag on both branches) | OPEN, worked around at CP-21 |
| F-14 | BLOCKER | **CP-26 (the stranger test) rows begin here** — There is no documented way to obtain the inputs. The run book's first command is `bash install.sh`, but the examples repo has NO git remote (its own RUNBOOK, bank, and scripts are unfetchable from anywhere), and the library repo — needed twice, for the wheel build AND for `vendor/polar/.venv` server-side — is unpublished on PyPI (wishlist 17); its GitHub mirror (github.com/MHGanainy/gsj-harness-rollout-server) is public but STALE at CP-19, six commits behind, predating this entire consumer surface, and is named in no document a stranger holds. CP-26 side-loaded both trees by operator hand (`git archive \| ssh tar -x`) | operator/estate (publish, or push the mirror current) | **RETIRED at library CP-29**: `v0.1.0` published to PyPI (proof: `pip install gsj-harness-rollout-server` in a scratch venv outside every repo, no index flags, then `checks.validate_session_result` on the real CP-09′ body → `[]`); the library mirror pushed current (CP-28, `1565813`); THIS repo given a public remote and pushed (github.com/MHGanainy/gsj-harness-rollout-server-examples). install.sh now installs from PyPI by default (sibling wheel = developer override); RUNBOOK's "What you need in hand" names the clone URLs. What remains operator-held is estate knowledge (F-22's secret, F-23's revision), not the inputs |
| F-15 | FRICTION | The wheel-build cure was un-runnable as printed. install.sh requires the library repo as a SIBLING checkout (`../../gsj-harness-rollout-server/dist/*.whl`) — stated nowhere ahead of failure, and the examples README actively said "the library arrives as a wheel, not a checkout"; the printed cure `(cd … && python -m build --wheel)` failed three ways on the stock box (`python` absent, no `build` module, no system pip). Workaround the stranger had to invent: use the venv install.sh itself just created | no — example-side | **FIXED at CP-26**: install.sh prints venv-based copy-pasteable commands, RUNBOOK states the layout, README carries the honesty rider |
| F-16 | FRICTION (blocking for the GPU step until cured) | The torch/driver trap fires in three acts and the cure was written nowhere. (1) install.sh silently lands the cu13x default (version string plain `2.13.0`, no local tag); (2) the final import check prints CUDA "driver too old (found version 12080)" and then `install OK` in the same closing block; (3) requirements.txt named `torch==2.13.0+cu126` but no mechanism — and `pip install torch==2.13.0 --index-url …/cu126` NO-OPS in 2.4s (the tagless installed version satisfies the spec). Working form requires the explicit `+cu126` tag. Cost: the desk halves run fine and the RuntimeError waits for the first CUDA call of the GPU step, with torch's own advice (update the driver) unusable on a shared box | no — example/docs | **FIXED at CP-26**: install.sh warns at install time when a GPU is present and torch.cuda is unusable, printing the exact cure; requirements.txt and RUNBOOK carry the command and the local-tag pitfall |
| F-17 | FRICTION | The stated verl closure was the DESK closure; the GPU path needs three more packages. `loop.make_worker` → `verl.workers.engine_workers` → `checkpoint_engine` → `rollout.utils` imports **uvicorn** then **fastapi**; the FSDP worker needs **peft**. Three ModuleNotFoundError crashes, one per invocation; CP-21's venv carried them by accident, so the measured "five the closure doesn't state" was itself one environment short of true | no — example-side | **FIXED at CP-26**: requirements.txt gains the three with the provenance comment; install.sh's import check still exercises only the desk closure (checking the GPU path would import the world — documented instead) |
| F-18 | FRICTION (trust) | The run book's collection expectations primed the wrong pain. Its headline "collection costs multiples of what it yields" + "19 attempts for 1 qualifying episode" + "~36 episodes/hour" set a stranger up for attrition; measured at CP-26: **71/72 attempts qualified in ~9.5 min** (Polar pooled 6 containers). The text DID attribute 19:1 to the strict standard and CP-21's 112 to reward-not-pipeline, and its 27/28 relaxed figure matches today's 98.6% — but the calibrating figure was buried, the wall-clock was ~6× off for pooled collection, and nothing was dated; the measured stranger reaction was to distrust the qualification gate. The reward expectation (sparse, unstable rate) was exactly right: 1/71 landed inside the stated 1/27–1/112 band | no — run book | **FIXED at CP-26**: the bullet now leads with the relaxed-standard yield, dates both regimes, and corrects the wall-clock. **[CP-31] Kept true through the mode flip**: every CP-26-era number was a thinking-OFF measurement, so the default flipping ON would have re-armed this exact failure (correct system, wrong-regime priming — a healthy ON collect runs ~2.7× the stated wall) — the run book's §Thinking now gives each mode dated, attributed expectations, labels extrapolations as such, and the off-labels are explicit throughout |
| F-19 | FRICTION | "Copy `config.yaml` and edit" vs train.py's `--config` default (`config.yaml`) and run-book commands that pass no `--config`: the literal reading edits a copy nothing reads. On the reference estate the unedited example crashes `gsj-rollout serve` at startup mkdir of root-owned `/data/gsj/traces` — a failure that reads as permissions, not as the-wrong-file; on any other estate everything fails while the edits sit unread. The config header itself repeated "Copy this file" | no — run book + example config | **FIXED at CP-26**: both now say edit-in-place and why |
| F-20 | FRICTION | The session's only instructions vanish when the server is backgrounded: `gsj-rollout serve`'s topology print (the rendered path + the two Polar commands) is block-buffered python stdout — under `nohup … > log` the log holds only the pins warning while the receiver listens silently. Foreground (or PYTHONUNBUFFERED=1) shows it | **yes** — cli.py should flush/line-buffer before serving | run book FIXED (foreground-first instruction); the flush is library wishlist 22 |
| F-21 | FRICTION | `vendor/polar/.venv` is assumed everywhere and provisioned by no gsj-authored document. The printed Polar commands reference it RELATIVE to an unstated cwd (the library repo root); the venv is absent from any fresh checkout (`.venv/` gitignored); the only provisioning recipe is Polar's upstream README — reachable by following the printed path into the vendored tree — and it omits nothing the printed PYTHONPATH doesn't cover, but assumes **uv**, which the run book's own A-14 line also assumed and which was not on the box | half — cli.py could print absolute paths + a missing-venv hint (wishlist 22) | run book FIXED: provisioning paragraph, cwd stated, uv declared a prerequisite |
| F-22 | FRICTION | The MCP secret has no acquisition or verification path. Gateway and service must share `GSJ_MCP_TOKEN_SECRET`; on an inherited estate nothing said where the running service's value lives (operator handover), and there is no way to test a candidate against the running service before an episode spends itself on failed tool calls | half — a verify-the-secret probe is mcp-service-side (wishlist 24) | run book FIXED: the secret is on the handover list, the no-probe hazard named |
| F-23 | FRICTION | `--snapshot <pinned HF dir>`: neither the fetch command, nor what "pinned" pins to, nor the consequence of drift was written. The engine's revision is estate lore (staging/); the config carries no revision field; CP-26's unpinned `hf download` matched the engine byte-for-byte by LUCK. Drift would surface only as an elevated replay delta — and the replay printout (`mean\|Δ\|=0.0137 vs floor 0.008` this run, on identical weights) had no stated reading semantics | half — an `estate.model_revision` key would let the pin travel in the YAML (wishlist 23) | run book FIXED: fetch command with `--revision`, the reference pin, and order-of-magnitude reading guidance |
| F-24 | COSMETIC | "You only need this project and the four URLs" vs Configure's SIX values; `estate.model` is a name and `traces_dir` a path, not URLs | no | **FIXED at CP-26** (run book counts honestly) |
| F-25 | COSMETIC | A required-key line deleted whole leaves a comment-only section that parses as YAML null, and `load_config` reports pydantic's "'polar.gateway': Input should be a valid dictionary or instance of GatewayNodeConfig" — naming no field and no cure (the unknown-key and missing-leaf errors, by contrast, are excellent) | **yes** — normalize None sections to {} so the field-level message fires (wishlist 23) | OPEN |
| F-26 | COSMETIC | The example's `traces_dir: /data/gsj/traces` was un-creatable on the very box the example mirrors (`/data` root-owned): the one SUPPLY value whose example text could not work on the reference estate, failing at `gsj-rollout serve` startup mkdir | no — example config | **FIXED at CP-26** (obvious placeholder + writability comment) |
| F-27 | COSMETIC | The promised "reward distribution" print is one `reward=` line per body (71 of them), no aggregate — the stranger greps and counts to learn "1/71"; same for collect (71/72 and the quarantine count were hand-computed) | example-code (train.py summary lines — wishlist 25) | run book FIXED (says what prints); code FIXED CP-27: train.py prints a `collect total: X/Y qualified` line and a `reward distribution: k/N nonzero, mean, max` line — nothing left to hand-count |
| F-28 | COSMETIC | After the `--no-deps` verl install, any env-CHANGING pip install prints an ERROR-labelled resolver block naming verl's five undeclared wants (hydra-core, pybind11, pylatexenc, tensorboard, wandb — hydra-core/pybind11 join after further installs shrink the set) — alarming, deliberate (ADR-0023), explained nowhere at the point it appears; fully-satisfied no-op installs print nothing, which made F-16's silent no-op doubly quiet | no — docs | **FIXED at CP-26** (requirements.txt + run book name the noise and the response) |
| F-29 | FRICTION (blocking for the sync leg) | The run book sent the sync to the WRONG SIDE: "Then the sync, estate-side" — but `staging/serving/serve-updated.sh` drives the estate over ssh via `GSJ_VLLM_SSH_HOST` (default alias `h200-admin`) and only works where that alias resolves, i.e. the operator workstation. Run on the estate box it announces "stopping the current engine" then dies `ssh: Could not resolve hostname h200-admin` (exit 255) — reading as broken DNS; the engine survives only because the stop itself sat behind the failed ssh. train.py's closing printout repeats "estate-side" | half — train.py's printed pointer (wishlist 25); the script is a workstation tool and correct as one | run book FIXED: workstation-side stated, the alias/env named, the misleading failure described; train.py's closing printout FIXED CP-27 (says WORKSTATION-side, names GSJ_VLLM_SSH_HOST) |
| F-30 | FRICTION | The Run commands did not run as written: bare `python` does not exist on the box, `gsj-rollout` lives only in the never-activated `.venv`, and no document said `source .venv/bin/activate` or prefix `./.venv/bin/` | no — run book | **FIXED at CP-26** (commands prefixed, the assumption named) |
| F-31 | FRICTION | install.sh ran its wheel gate LAST: the full multi-GB requirements closure installed before the check that aborts the run (masked in CP-26's timing by a warm pip cache; a cold box pays ~20 min to reach an inevitable error) | no — example-side | **FIXED at CP-26** (gate hoisted before any download) |
| F-32 | FRICTION | The run book presented `example_project/` as the project, but train.py is dead without the SIBLING `verl_bridge/` (conversion) and `slime_bridge/` (grader) directories — copying the one directory breaks at import | no — run book | **FIXED at CP-26** (move the whole repo, stated up front) |
| F-33 | FRICTION | "Estate bring-up is the library repo's `staging/` recipes" was wrong: `staging/` holds only this estate's deltas and the serving scripts; the authoritative cold-start walk (BRINGUP.md) lives in the PREDECESSOR repo `gsj-envloader`, which a stranger neither holds nor is told exists | no — run book | **FIXED at CP-26** (pointer corrected, third repo named) |
| F-34 | FRICTION | GPU selection is undocumented and hardcoded: the worker takes `cuda:0`, the run book said "discover free GPUs at run time" with no mechanism, and CP-26's success was tenancy luck (GPU 0 happened to be free on a shared box) | example-code (a device arg — wishlist 25) | run book FIXED (CUDA_VISIBLE_DEVICES instruction); code FIXED CP-27: `--gpu N` sets CUDA_VISIBLE_DEVICES before the first CUDA call (default still first-visible, now stated in --help) |
| F-35 | FRICTION | The GPU leg's observability: ~21 minutes of near-silence whose most visible line is a `CUDACachingAllocator … memory allocation failed with OOM` WARNING (the allocator retrying — F-12/F-13's padded-width shape — not the crash it resembles); no stated wall-clock or ~90 GB footprint anywhere | no — run book | **FIXED at CP-26** (expectations paragraph: duration, footprint, the warning's meaning) |
| F-36 | COSMETIC | The A-14 instruction ("`uv pip install -p .venv/bin/python -e ../..` or the gateway dies") was stale: the printed Polar commands already inline a PYTHONPATH that satisfies the import — following the run book performed an unnecessary install | no — run book | **FIXED at CP-26** (rewritten to match what serve prints) |
| F-37 | COSMETIC | Traces land in TWO stores — the receiver's `traces_dir` (durable archive) and train.py's `collected/` (working set) — and no document named the relationship or which one is safe to delete | no — run book | **FIXED at CP-26** (the relationship paragraph) |
| F-38 | COSMETIC | `bash rebuild_taskbank.sh` cannot run as written: it needs a library-repo venv no documented trainer step creates (and rebuilding is rarely wanted — the shipped parquet is the bank) | no — run book | **FIXED at CP-26** (stated beside the command) |
| F-39 | COSMETIC | The packaged-pins REFERENCE-ESTATE warning prints in full on EVERY import — install, serve, every train.py run — threatening "every hash gate fails", and on the reference estate the correct response is to do nothing; the run book said "set nothing" without saying the warning would keep shouting | half — a warn-once or estate-matched silence is checks.py-side (noted in wishlist 22's row) | run book FIXED (the warning's persistence named). **[CP-31] Scope narrowed by the thinking default**: the shipped default (thinking ON) sets `GSJ_PINS_PATH` on both legs, and the warning fires only when the variable is UNSET and the packaged copy resolves — so the default path no longer prints it at all; off-mode (control) runs still do |
| F-40 | **CP-31 (thinking default flip) rows begin here** — FRICTION (BLOCKER-shaped for a pip-only thinking-on consumer until CP-31's interim cure) | The wheel ships ONLY the thinking-off reference pins (library ADR-0019/ADR-0024 posture: packaged/default resolution *means* thinking-off; `pins/thinking-on/` lives in the library repo's checkout and is not packaged). With this project's default flipped to ON, a `pip install`-only consumer holds no thinking-on pins anywhere on disk — default-on + unset `GSJ_PINS_PATH` = every first-run episode quarantined G6-only, a first run that produces nothing with a finding the stranger cannot interpret | **yes** — the durable cure is the wheel carrying both mode files as package data with a documented packaged path (library wishlist row 28); ADR-0024's "default resolution keeps meaning thinking-off" can survive that unchanged | **INTERIM CURE at CP-31, examples-side**: `example_project/pins/thinking-on/pins.gsj.json` is a byte-identical copy (provenance + drift story: `pins/README.md`); train.py auto-selects it for its own leg and refuses mode/pins mismatches in words; the RUNBOOK serve command carries `GSJ_PINS_PATH` inline for the receiver leg. Residual risk the interim carries: the copy can drift when the library re-pins — train.py's preflight compares the six non-G6 sets against the installed wheel's reference pins and warns in words |
| F-41 | DOC | `GSJ_PINS_PATH` is resolved ONCE per process, at the first `gsj_rollout.checks` import (`checks.py:118`, library CP-11b) — so a consumer script that imports the library at module top has already fixed its pins before any config is read, and setting the variable afterwards silently does nothing. Invisible to a naive consumer; the symptom would be the F-40 quarantine wearing a "but I set the variable" face | half — the library could re-read the env var at first *use* rather than first import (or document the constraint on the consumer surface); today it is a comment in `checks.py` and a RUNBOOK sentence | **HANDLED in train.py at CP-31**: all `gsj_rollout`/bridge imports are deferred behind the thinking/pins preflight (`_heavy_imports()`, the module docstring says why); the RUNBOOK's foreign-estate paragraph now names the once-per-process fact. Any OTHER consumer script must copy the same ordering |
| F-42 | DOC | The two bridge READMEs carry mode-unlabelled off-era numbers and never mention the mode/pins coupling: `verl_bridge/README.md` ~95 states the ≈19-attempts collection cadence with no mode label (and its named alternative, the relaxed standard, numberless), `slime_bridge/README.md` ~53 the same figure, and `slime_bridge/README.md` ~20 a CP-16-era packaged-pins sentence that post-CP-30 is true only of the OFF reference set. A stranger planning a thinking-on collection from the bridge docs alone inherits off-mode expectations — the F-18 shape, relocated. The same off-era shape sits in the decision records (`slime_bridge/decisions/ADR-0001` ~87 repeats the 19-attempt figure unlabelled; `ADR-0002` ~17 frames comparability as four engine legs + one singular pins file, pre-dating per-mode pins AND unenumerating the engine's own fifth thinking switch, `--default-chat-template-kwargs '{"enable_thinking": false}'`, in the qualified argv since CP-04′) | no — examples-side docs | **MITIGATED at CP-31**: a mode-note rider at the top of each README labels every number off-measured and points at the RUNBOOK's §Thinking; the in-body prose is CP-16/CP-17/CP-20/CP-21 evidence and stays verbatim (the loops those documents describe DID run off-mode), and the ADRs stay untouched — append-only decision history; a superseding note there is a new ADR's job, not an edit's |
| F-43 | DOC | Every replay-floor constant is an unlabelled thinking-off measurement: `verl_bridge/bridge.py`'s `H200_REPLAY_FLOOR_MEAN` 0.008 / per-position 0.21 (docstring means 0.005246/0.007141) date from CP-09′, and CP-26's 0.0137 composition-variance reading was an off-mode batch. NO thinking-on batch has crossed the GPU leg yet, so under the shipped default the run book's order-of-magnitude reading guidance is UNVALIDATED in both directions: the band could absorb a real ON-regime problem or flag a healthy ON batch. The same unlabelled floors recur across the loop docs (`verl_bridge/README.md` ~106, `verl_bridge/decisions/ADR-0004` ~64 — CP-17's 0.0088 slime recompute included); all print-only diagnostics, so the exposure is F-18-style misreading, never a hard failure | no — constants are examples-side; the validation needs an estate | OPEN — RUNBOOK's replay bullet now carries the rider; the constants stay unlabelled in code (deliberate: re-measuring, not re-labelling, is the cure — CP-32 runs the ON leg) |
| F-44 | DOC (latent FRICTION, mode-amplified) | `slime_bridge/reward_cited_pages.py` grades only `sorted(glob("*.md"))[0]` of the artifacts dir and silently ignores every other `.md`. Not mode-ASSUMING — but thinking-on writes deliverables 8/15 vs 1/15 (CP-28), so multi-file artifact dirs get proportionally likelier under the shipped default and the graded file is whichever sorts first, with no line saying others were skipped | no — grader design (the predecessor's shape — an explicitly declared per-record artifact path, `gsj-envloader-examples` `rlvr/grade.py:66` — is the durable fix) | OPEN, recorded at CP-31; unfixed deliberately (a grading-semantics change mid-flip would confound CP-32's A/B) |
| F-45 | **CP-32 (the stranger test, both modes) rows begin here** — FRICTION (BLOCKER-shaped without the §Run hint) | Under a PyPI install, `gsj-rollout serve`'s two printed Polar commands resolve `vendor/polar/.venv/bin/polar` against the installed package and print an ABSOLUTE path into site-packages (`…/site-packages/vendor/polar/.venv/bin/polar`) that does not exist — no wheel ships `vendor/` (verified: ENOENT, 2026-08-14). Run as printed: "No such file or directory". The RUNBOOK's pre-CP-32 F-21 sentence ("a path RELATIVE to the library repo root … run them with the library repo as your cwd") described the checkout-era print and no longer matched the output; recovery required hand-substituting the sibling checkout's binary while keeping the printed `PYTHONPATH` | **yes** — `cli.py` should detect an installed-wheel layout and print the placeholder + words (or a `--polar-root` knob) instead of a path it cannot know | RUNBOOK §Run rewritten at CP-32 (keep the printed PYTHONPATH, take the binary from the sibling library checkout); the print itself is library wishlist row 29 |
| F-46 | COSMETIC | `train.py` under `nohup … > log` printed nothing for an entire 13-minute collect — python block-buffers redirected stdout (F-20's shape on the trainer side; the RUNBOOK warned it for `serve` only). A stranger tailing the log cannot tell a slow ON collect from a hang — exactly the doubt the §Thinking cost-banner exists to prevent | no — example-side | **FIXED at CP-32**: `sys.stdout.reconfigure(line_buffering=True)` at the top of `main()`; RUNBOOK trainer-side note updated |
| F-47 | DOC (latent FRICTION at scale) | At the default collect's scale the 32k wall is HIT and qualification does not screen it: 7/72 thinking-on episodes ended `finish_reason: length` (max merged row 32,645 of 32,768; 5 episodes >90% of window) and ALL SEVEN qualified and entered the training set silently (2026-08-14; CP-28's n=15/leg had measured zero and the run book said so). A trainer inherits truncated episodes with no line saying so | half — a `checks` policy option could reject or flag length-terminated episodes (H-41's shape); whether to train on them is a consumer decision | RUNBOOK §Thinking + §what-to-expect carry the measured rate and the "qualification does not screen length" sentence since CP-32; the checks option is library wishlist row 30 |
| F-48 | DOC (latent FRICTION for archive re-use) | The receiver's `traces_dir` is not per-mode: across a serve restart both modes' accepted traces land in ONE archive (144 files after the CP-32 A/B), distinguishable only by content. The `--out` discipline protects the working set; nothing separates the durable archive, and re-grading an archived mix later reproduces F-49's undiagnosable masked-rows failure | half — the receiver could stamp the mode into the layout (per-mode subdir or filename token); the doc cure is consumer-side | RUNBOOK §Thinking item 3 says "point each mode at its own traces_dir" since CP-32; the receiver stamp is library wishlist row 31 |
| F-49 | FRICTION | The mixed-directory mistake does NOT fail as documented: §Thinking promised "G6-named findings on the foreign-mode half", but grading the ON directory under `--thinking off` produced NO G6-named output anywhere — every body ingested with its rows silently masked untrainable, all 72 per-body reward lines printed (identical rewards; grading is mode-agnostic), and the run died at `AssertionError: 72 masked rows in a qualifying collection` (train.py:382) — naming neither G6 nor pins nor mode. The §Thinking-cited "code comment" sits at the ingest callsite, not at the assert that fires. A stranger cannot reach the diagnosis from those words (2026-08-14, TRAIN_RC=1) | half — the bridge/`checks` ingest path masks instead of surfacing the G6 findings it computed; a named error (or findings on the return) is the durable cure (library wishlist row 30's sibling, same row) | **MITIGATED at CP-32**: the assert message now names the mode/pins hypothesis when ALL rows are masked, and §Thinking describes the real symptom; the library-side surfacing stays open |
| F-50 | COSMETIC (DOC) | The unquoted `thinking: on` trap fires as a raw pydantic traceback, not the documented named rejection: the config comment claimed bare `on` "becomes the string 'on' … rejected at load by name"; measured, pyyaml yields boolean `True` and the validator refuses the TYPE (`harness.thinking Input should be a valid string [input_type=bool]`). train.py's own pre-read banner meanwhile prints "thinking: on" (it maps the bool), so the banner and the validator disagree about the same byte. Findable only because the KEEP-THE-QUOTES comment sits beside the edited line | half — the validator could map/name YAML-1.1 booleans (accept `True` as "on" with words, or emit the by-name sentence the comment promised) | config.yaml's comment corrected to the measured behaviour at CP-32; the named-message half is library wishlist row 32 |
| F-51 | DOC | `collect total: … (0 rejected/quarantined)` counts THIS process's verdicts only — during the receiver-leg pins mismatch (the injected item-2 run) it read "9/9 qualified, 0 rejected/quarantined" with RC=0 while the durable archive gained nothing and the receiver quarantined all 9 G6-only. The one healthy-looking line a stranger trusts most points away from the one failure train.py structurally cannot see | no — example-side wording | **FIXED at CP-32**: the line says "client-side" and a follow-on line sends the reader to check `traces_dir` after a first collect |
| F-52 | FRICTION (process; cured mid-CP) | Both repos' CP-31 commits were UNPUSHED when CP-32 began — the "both repos are public" premise was one checkpoint stale, and a stranger cloning before the operator's push would have received the CP-29 surface (default OFF, no §Thinking, no pins copy). Cured by pushing before the stranger clone; recorded because nothing in the CP protocol makes "landed" imply "published" | no — process (a push step in every CP that ships consumer-visible files) | pushed at CP-32 before the measurement; the protocol sentence is the operator's to add |
| F-53 | COSMETIC (verified) | `staging/serving/serve-updated.sh:81` — `ssh "$SSH_HOST" bash -c '<newline>…'` word-joins so the remote `bash -c` receives an EMPTY first line as its whole script: it prints `bash: -c: option requires an argument` and the health-wait body then runs in the remote login shell BY ACCIDENT (it worked: the wait and the /v1/models check both executed; sync verified 0.040184 mean|Δ|, 7327/7609 positions moved). Works-by-luck in a frozen path | yes — `staging/` (frozen; one-line fix: `bash -s` or quote the whole remote command) | OPEN, verified at CP-32; library wishlist row 33 |
